# Playwright screenshots

OpenLAG 0.5.x P3 generates deterministic documentation screenshots with:

```bash
npm run docs:screenshots
```

Generated files are written to:

`docs/assets/screenshots/generated/`

Current expected outputs:

- `portal-shell.png`
- `portal-docs-view.png`

These binary outputs are reproducible and intentionally ignored by Git.
